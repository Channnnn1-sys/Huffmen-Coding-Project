# Huffman File Compression Web Application

## Overview

<<<<<<< HEAD
This project is a web-based Huffman compression system powered by a native C++ computational engine with Flask serving as the lightweight interface and deployment layer.
=======
The Huffman File Compression Web Application centers on a native C++ compression engine while Python Flask acts as a lightweight orchestration and web layer. The C++ engine performs the core work (frequency analysis, Huffman tree construction, encoding/decoding, hashing, entropy calculation, header generation, and JSON report output). Flask handles uploads, creates temporary session directories, invokes the C++ binaries as subprocesses, and forwards generated reports and archives to clients.
>>>>>>> 43f042d (C++ codes increased!)

The application uses compiled C++ for the real Huffman compression and decompression work, while Flask handles uploads, HTTP routing, temporary session directories, JSON responses, and browser download packaging. It is designed for Render/Linux deployment and follows a stateless cloud architecture in which uploads are processed in temporary session directories and cleaned up automatically.

<<<<<<< HEAD
TXT files are the primary effective evaluation format because Huffman coding performs best on text-like data with repeated symbols and low-to-moderate entropy. Binary formats such as PDF, DOCX, JPG, and MP4 may not compress effectively because they often already contain high entropy, internal compression, or highly random content, which limits the usefulness of prefix-free Huffman coding.

This project remains intentionally simple and academic: Flask handles HTTP, templates, routing, and ZIP packaging, while the compiled C++ engine performs the actual computational work. The Python side is kept lightweight so the system stays easy to deploy on Render/Linux and easy to explain in a research or defense setting.
=======
This project is both educational and practical: the C++ engine demonstrates algorithmic implementation and performance considerations while the Flask layer demonstrates safe, stateless web orchestration.
>>>>>>> 43f042d (C++ codes increased!)

---

# Features

## Core Features

* Huffman Coding based file compression
* Huffman Coding based file decompression
* Multi-file batch processing
* Drag-and-drop file upload interface
* Cross-platform support
* Temporary session-based file handling
* Automatic cleanup of processed files
* Compression statistics and reports
* Responsive web interface
* Render deployment support

---

## Supported File Types

### Compression

The application accepts many file types for demonstration, but the most reliable compression results are expected on TXT, CSV, LOG, JSON, XML, and source-code files.

Binary or already-compressed content such as PDF, DOCX, JPG, PNG, MP3, MP4, and ZIP may not reduce in size meaningfully because the data already has high entropy or internal compression.

Examples of practical input types include:

* TXT
* DOC
* DOCX
* PPT
* PPTX
* XLSX
* PDF
* CSV
* LOG
* JSON
* XML
* Images
* Binary files
* Source code files

Compression efficiency depends on the contents of the file.

### Decompression

The decompression page accepts:

* `.bin` files generated by this application
* ZIP packages generated by this application

For best results, users should decompress files that were originally compressed using this system.

---

# Technology Stack

## Backend

* Python 3
* Flask
* Werkzeug

## Compression Engine

* C++
* Huffman Coding Algorithm
* Binary File Processing

## Frontend

* HTML5
* CSS3
* JavaScript

## Deployment

* Render
* Gunicorn
* Linux

---

# System Architecture

```text
Browser
   │
   ▼
Flask Application
   │
   ▼
Temporary Session Directory
   │
   ▼
C++ Huffman Compressor / Decompressor
   │
   ▼
ZIP Packaging
   │
   ▼
Base64 Encoding
   │
   ▼
JSON Response
   │
   ▼
Browser Download
```

The application uses a stateless cloud-friendly architecture suitable for Render and Linux hosting.

No uploaded files remain on the server after processing is complete, and the C++ engine remains the main computational component behind the Flask interface.

---

# Compression Workflow

1. User uploads one or more files.
2. Flask saves files into a temporary session directory.
3. File analysis is performed.
4. Entropy analysis is generated.
5. Office file deep scanning is performed when applicable.
6. The C++ Huffman compressor is executed through a subprocess.
7. Compressed output is generated.
8. Compression statistics are calculated.
9. Results are packaged into a ZIP archive.
10. ZIP archive is encoded into Base64.
11. Browser automatically downloads the result.

---

# Decompression Workflow

1. User uploads a compressed `.bin` file or generated ZIP package.
2. Flask saves the file into a temporary session directory.
3. ZIP contents are extracted when necessary.
4. The C++ Huffman decompressor is executed.
5. Original file is reconstructed.
6. Results are packaged into a ZIP archive.
7. Browser downloads the restored file.

---

# Entropy Analysis

The application performs Shannon Entropy analysis on uploaded files.

Entropy helps estimate how compressible a file is.

Examples of files with high entropy:

* PDF
* ZIP
* RAR
* JPG
* PNG
* MP4
* MP3

When entropy is high, the application warns users that additional compression may produce limited savings.

---

# Office Deep Scan

The application performs additional analysis for:

* DOCX
* PPTX
* XLSX

These file types are ZIP-based Office containers.

The system scans XML content inside:

* Word documents
* PowerPoint slides
* Excel workbooks

This analysis provides insight into how much textual content exists within the file and helps explain expected compression performance.

---

# Security Features

The application includes several security protections:

* Secure filename sanitization using `secure_filename()`
* Maximum upload size enforcement
* Temporary session directories
* Automatic cleanup after processing
* Safe ZIP extraction
* Input validation
* Error handling for corrupted files
* Execution timeout protection

---

# Project Structure

```text
project/
│
├── main.py
├── requirements.txt
├── README.md
├── render.yaml
│
├── compressor/
│   ├── huffcompress.cpp
│   ├── huffdecompress.cpp
│   ├── huffcompress.exe
│   ├── huffdecompress.exe
│   ├── huffcompress
│   └── huffdecompress
│
├── templates/
│   ├── index.html
│   ├── compress.html
│   ├── decompress.html
│   └── about.html
│
├── static/
│   ├── css/
│   ├── js/
│   └── assets/
│
├── tests/
│
├── compile_binaries.py
├── compile_linux.sh
└── verify_suite.py
```

---

# Installation

## Clone Repository

```bash
git clone <repository-url>
cd project
```

---

## Create Virtual Environment

### Windows

```bash
python -m venv venv
venv\Scripts\activate
```

### Linux / macOS

```bash
python3 -m venv venv
source venv/bin/activate
```

---

## Install Dependencies

```bash
pip install -r requirements.txt
```

---

# Build C++ Compression Binaries

## Windows

```bash
cd compressor

g++ -O2 -o huffcompress.exe huffcompress.cpp
g++ -O2 -o huffdecompress.exe huffdecompress.cpp
```

## Linux / macOS

```bash
cd compressor

g++ -O2 -o huffcompress huffcompress.cpp
g++ -O2 -o huffdecompress huffdecompress.cpp
```

---

# Running Locally

```bash
python main.py
```

Application URLs:

```text
Home:
http://localhost:5000

Compression:
http://localhost:5000/compress

Decompression:
http://localhost:5000/decompress

About:
http://localhost:5000/about

Debug:
http://localhost:5000/debug
```

---

# Render Deployment

The application is designed to run on Render.

Deployment process:

1. Push repository to GitHub.
2. Connect repository to Render.
3. Render installs dependencies.
4. Render compiles Linux Huffman binaries.
5. Gunicorn starts the Flask application.

The included `render.yaml` automates deployment configuration.

---

# API Routes

| Route         | Method | Description            |
| ------------- | ------ | ---------------------- |
| `/`           | GET    | Home page              |
| `/compress`   | GET    | Compression page       |
| `/compress`   | POST   | Compress files         |
| `/decompress` | GET    | Decompression page     |
| `/decompress` | POST   | Decompress files       |
| `/about`      | GET    | About page             |
| `/debug`      | GET    | Deployment diagnostics |

---

# Performance Notes

Compression performance depends on:

* File size
* File type
* Data entropy
* Available CPU resources

Typical observations:

* Text files compress very well.
* Source code compresses well.
* CSV and logs compress well.
* Images and videos often compress poorly because they are already compressed.

---

# Limitations

Current limitations include:

* Files are processed sequentially.
* Very large files may require longer processing times.
* Compression effectiveness varies by file type.
* Base64 packaging increases memory usage during transfer.
* Huffman Coding alone cannot outperform modern archive formats such as ZIP, 7-Zip, or RAR on already compressed files.

---

# Future Improvements

Potential future enhancements:

* Adaptive Huffman Coding
* Parallel processing
* Streaming file transfers
* Compression benchmarking
* User accounts
* Compression history
* Docker deployment
* Automated testing pipeline
* Advanced analytics dashboard

---

# Project Team

### Arman Christian Malgapo

Lead Developer & Backend Systems Programmer

Responsible for system development, Flask integration, application architecture, deployment, debugging, and overall implementation.

### Nikko Madueño

Quality Assurance & Testing Specialist

Responsible for testing, validation, bug identification, and quality verification.

### Paul Henrick Maquiniana

Project Coordinator & Development Manager

Responsible for project planning, organization, scheduling, and coordination.

### Theo Masato Nakajima

Technical Documentation & Research Specialist

Responsible for documentation, technical writing, research, and project records.

---

# Educational Purpose

This project was developed primarily for educational and academic purposes to demonstrate:

* Huffman Coding
* Data Compression
* C++ Programming
* Python Flask Development
* Web Application Design
* Client-Server Communication
* Cloud Deployment
* Cross-Platform Development

---

# Version History

## Version 2.0

Major improvements:

* Cloud-native architecture
* Render deployment support
* Linux compatibility
* Temporary session processing
* Batch file processing
* Entropy analysis
* Office Deep Scan analysis
* Compression reports
* Cross-platform binary detection
* Improved error handling
* Security improvements
* Responsive user interface
* Automatic cleanup system

---

## Status

Current Status: Stable

Deployment Tested:

* Windows
* Linux
* Render

Last Updated: 2026
